# AGENTS.md — Onerway Payment Showcase

## 开始前

1. 先读 `docs/showcase-contract.md` 的产品目标与本次涉及的章节，它是本项目产品、支付状态和安全边界的唯一长期权威；同一任务中未变化的内容不重复通读。
2. 构建 UI 时读取当前 canonical `geist-nuxt` 的 `AGENTS.md`、`SKILL.md` 和相关 references。默认本地位置为同级目录 `../geist-nuxt`。
3. `docs/design/sdk-m0.html` 只是冻结的视觉与状态参考；若它与契约、API 文档、安全规则或设计系统冲突，以后者为准。

## 需求判断与范围

- 以客户能理解、能操作、能重复演示的参考商户体验衡量交付价值。Showcase 对自身的接入、状态展示、恢复和数据安全负责；Provider 托管的支付交互优先复用，不默认承担支付平台的完整认证、风控或渠道测试职责。
- 开始执行前，用简短说明明确本次用户可见的结果、必要改动和完成条件；小任务不另建计划文档。技术方案与验收项都应能解释它支持哪个当前需求，不能只因“还能测”或“行业通常需要”就加入本次交付。
- 结合用户最新明确意图、当前契约和关联 Issue 确定范围。Issue 中的历史待办、契约中的能力清单和既有实现约束，不自动意味着本次必须全部实现或全部完成真实验收。发现范围冲突时指出具体条目与影响；用户已明确纠正的内容直接落实并同步相关记录，仍不明确且影响交付边界的部分再确认。
- 用户纠正业务语义或目标后，先修正判断与受影响的计划，再继续执行；不要只追加解释、保留已失效的待办，也不要顺势移除用户没有否定的其他要求。区分已知事实、用户确认和推测，不从按钮名称或单笔现象推导支付协议。

## 验收与停止条件

- 验证强度与本次改动及风险匹配：用自动化检查 Showcase 的状态映射、关联、幂等与权限等稳定规则；用真实浏览器检查受影响的用户操作；用真实 Sandbox 验证本次承诺的支付旅程和必要外部链路。遵守仓库必需检查，但不要求每条自动化异常用例都有真实交易副本。
- 每个缺口说明是否阻断本次交付及原因。阻断应有依据：当前承诺的演示无法完成、已发现状态错误或重复创建风险、安全问题，或明确约定的必验项未完成。仅因 Provider 拒付策略、渠道异常或设备组合尚无真实样本，不自动升级为 Demo 的交付阻断；若本次需求明确涉及，则验证对应边界。
- 自动化 / mock、用户人工确认、真实 Sandbox 操作、通知重发与手动回放分别记录，只证明实际观察到的范围。缺少真实证据时保留“未验证”，不改写为通过；“未验证”也不自动等同于“本次未完成”。能力标为 Available 仍遵守契约要求。
- 完成约定范围且相关检查通过后，进入审查与交付，不继续寻找新场景延长任务。仅在新改动、失败或未解决的具体风险出现时扩大或重复验证；排查优先复用已知订单和已有证据，需要新样本时说明其必要性并遵守交易授权边界。

## 协作与收尾

- 已授权范围内的可逆实现和检查直接推进；只把确需用户业务判断、权限或设备配合的步骤交给用户，说明具体原因。不要反复询问已确认事项，也不要用连续建议代替完成已经接受的工作。
- 用户问“接下来做什么”时，先判断本次是否已经可以收尾，再给出最直接的下一步及其价值；不要默认从所有历史未验证项中继续挑选工作。明确区分当前必要动作与可选后续工作。
- `AGENTS.md` 只保存长期协作规则，产品与支付决定写入契约，任务范围和验收证据写入关联 Issue / PR；更新已有条目并去掉失效结论，不复制完整对话流水。交付说明包含结果、验证范围、实际阻断和需要的用户动作；未经明确授权不合并。

## 实现规则

- 技术栈为 Nuxt 4、Vue、TypeScript、Nuxt UI；不用 React。
- 设计基础只能通过 canonical `geist-nuxt` registry 安装和更新。不要复制旧 export、旧 starter、playground 或 API Docs kit。
- 初始仅消费 `geist-foundation`。新增设计切片前，先确认 registry 中已有对应条目。
- 业务样式只写入 `app/assets/css/app.css`；不要修改 registry 受管的 `app/assets/css/main.css`，并保持业务入口排在 foundation 之后。
- `ONERWAY_*` 配置只由 `server/utils/profile.ts` 解析；客户端只能消费 `/api/profile` 的显式白名单摘要，不得新增第二套 profile 解析或切换入口。
- 领域模型不依赖 Vue 组件。跨端支付模型按 `shared/payment/` 组织，优先使用精炼文件名，例如 `order.ts`、`attempt.ts`、`event.ts`、`capability.ts`。
- Checkout、Web JS SDK 和 Direct API 都适配到统一的 `Order → PaymentAttempt → PaymentEvent` 模型，不在页面组件中分别发明状态机。
- GitHub Issue 描述“接下来做什么”；完成后仍长期成立的决定必须回写 `docs/showcase-contract.md`。

## 安全红线

- `secret` 及生产凭据只能存在于服务端环境变量，绝不进入 `runtimeConfig.public`、客户端 bundle、日志、Issue、截图或提交历史。
- PAN / CVV 即使来自 Sandbox，也不得持久化、埋点或记录日志。
- 所有原始请求、响应和 Webhook payload 在保存或展示前必须脱敏。
- Production profile 默认不可交易；启用条件未在契约中确认前不得自行放开。
- 客户端 SDK 回调和 `returnUrl` 不能单独作为最终支付成功依据。

## 防漂移

- 修改能力矩阵、支付状态、环境策略、回调真值或 M0 范围时，同一变更必须更新契约。
- 遇到文档、SDK 行为和 API 响应不一致时，先记录证据并暂停依赖该不确定事实的实现，不凭推测补契约；不受影响的已授权工作继续推进。
- 不创建与契约重复的 `ROADMAP.md`、`architecture.md`、`payment-model.md` 或零散 ADR；只有替换已上线架构决定时才新增 ADR，并在契约中声明替代关系。
